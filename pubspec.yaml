name: my_login_app
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0" # Dart SDK 버전 범위 수정

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.16.0 # 최신 안정 버전으로 업데이트
  firebase_auth: ^5.7.0 # firebase_core와 호환되는 버전
  google_sign_in: ^6.1.0 # 최신 안정 버전 유지
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
